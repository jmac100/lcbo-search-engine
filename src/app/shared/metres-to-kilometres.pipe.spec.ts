/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MetresToKilometresPipe } from './metres-to-kilometres.pipe';

describe('MetresToKilometresPipe', () => {
  it('create an instance', () => {
    let pipe = new MetresToKilometresPipe();
    expect(pipe).toBeTruthy();
  });
});
